version: '3'
services:
  codeql-webapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    # volumes:
    #   - .:/app
    # environment:
    #   - FLASK_ENV=development
